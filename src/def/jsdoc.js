/**
 * Information sent by clients to create the connection tunnel
 *
 * @typedef {Object} ClientInfoObj
 * @property {number} connType - type of connection
 * @property {string} srcHost - source host
 * @property {string} dstHost - destination host
 * @property {number} dstPort - destination port
 * @property {string} [srcPath] - source path regex matching rule
 * @property {string} [dstPath] - dst path regex matching rule
 * @property {number} [timeToLive] - TTL for renew the registration
 * @property {string} [signature] - client signature
 * @property {boolean} [isLocal] - Deprecated: unused
 */

/**
 * Void Callback
 *
 * @callback VoidCallback
 */

module.exports = {};
